```cpp
class Solution {
public:
    int largestRectangleArea(vector<int>& heights) {
        int n = heights.size(),res = INT_MIN;

        vector<int> left(n,-1),right(n,n);

        stack<int> st ;

        for(int i = 0;i<n;i++)
        {
            while(st.size() && heights[st.top()] >= heights[i])
            {
                st.pop();
            }

            if(st.size()) left[i] = st.top();

            st.push(i);
        }

        st = stack<int>();

        for(int i = n-1;i>=0;i--)
        {
            while(st.size() && heights[st.top()] >= heights[i])
            {
                st.pop();
            }

            if(st.size()) right[i] = st.top();

            st.push(i);
        }

        for(int i = 0;i<n;i++)
        {
            res = max(res,heights[i] * (right[i] - left[i] - 1));
        }

        return res;
    }
};
```

```python
class Solution:
    def largestRectangleArea(self, heights: List[int]) -> int:
        n = len(heights)
        left,right = [-1] * n , [n] * n

        st = deque()

        for i,c in enumerate(heights):
            while len(st) and heights[st[-1]] > c:
                idx = st.pop()
                right[idx] = i
            st.append(i)
        print(right)

        st = deque()
        for i in range(n-1,-1,-1):
            c = heights[i]
            while len(st) and heights[st[-1]] > c:
                idx = st.pop()
                left[idx] = i
            st.append(i)
        print(left)

        res = [0] * n
        for i in range(n):
            res[i] = heights[i] * (right[i] - left[i] - 1)

        return max(res)
```
