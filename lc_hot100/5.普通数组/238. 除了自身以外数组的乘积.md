```cpp
class Solution {
public:
    vector<int> productExceptSelf(vector<int>& nums) {
        int n = nums.size();
        vector<int> res(n,0),a(n,1),b(n,1);

        for(int i = 1;i<n;i++)
        {
            a[i] = a[i-1] * nums[i-1];
        }

        for(int i = n-2;i>=0;i--)
        {
            b[i] = b[i+1] * nums[i+1];
        }

        for(int i  = 0;i<n;i++)
        {
            res[i] = a[i] * b[i];
        }

        return res;
    }
};
```

```python
class Solution:
    def productExceptSelf(self, nums: List[int]) -> List[int]:
        n = len(nums)

        pre = [1] * n
        nxt = [1] * n
        res = [1] * n

        for i,c in enumerate(nums):
            if i == 0:
                continue
            else :
                pre[i] = pre[i - 1] * nums[i - 1]

        for i in range(n-1,-1,-1):
            if i == n - 1:
                continue
            else :
                nxt[i] = nxt[i + 1] * nums[i + 1]
        for i in range(n):
            res[i] = pre[i] * nxt[i]

        return res


```
