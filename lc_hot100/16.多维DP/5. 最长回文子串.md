```cpp
class Solution {
public:
    string longestPalindrome(string s) {
        int n = s.size(),st = 0,le = 1;
        vector<vector<bool>> dp(n,vector<bool> (n,false));
        for(int i = 0;i<n;i++) dp[i][i] = true;
        for(int len = 2;len <= n; len ++)
        {
            for(int left = 0;left + len - 1 <n;left ++)
            {
                int right = left + len - 1;
                if(s[left] == s[right])
                {
                    if(len<=3)
                    {
                        dp[left][right] = true;
                        if(len > le)
                        {
                            le = len;
                            st = left;
                        }
                    }
                    else
                    {
                        dp[left][right] = dp[left + 1][right - 1];
                        if(dp[left][right])
                        {
                            if(len > le)
                            {
                                le = len;
                                st = left;
                            }
                        }
                    }
                }

            }
        }

        return s.substr(st,le);
    }
};
```

```python
class Solution:
    def longestPalindrome(self, s: str) -> str:
        n = len(s)
        res = s[0]
        dp = [[False] * n for i in range(n)]
        for i in range(n):
            dp[i][i] = True
        for l in range(2,n + 1):
            for left in range(0,n):
                right = left + l - 1
                if right >= n:
                    break
                if s[left] == s[right]:
                    if l <= 3:
                        dp[left][right] = True
                    else:
                        dp[left][right] = dp[left+1][right - 1]

                    if l > len(res) and dp[left][right]:
                            res = s[left:right+1]

        return res
```
