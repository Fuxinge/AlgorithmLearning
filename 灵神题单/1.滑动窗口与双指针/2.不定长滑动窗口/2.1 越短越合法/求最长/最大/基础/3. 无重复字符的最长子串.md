```python
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        mp = defaultdict(int)
        j = 0
        res = 0
        for i,c in enumerate(s):
            mp[c] += 1
            while j <= i and mp[s[i]] > 1:
                mp[s[j]] -= 1
                j = j + 1
            res = max(res, i - j + 1)
        return res
```
