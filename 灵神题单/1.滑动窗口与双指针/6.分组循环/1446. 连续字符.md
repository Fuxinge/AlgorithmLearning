```python
class Solution:
    def maxPower(self, s: str) -> int:
        l = r= res = 0
        while l < len(s):
            while r < len(s) and s[r] == s[l]:
                r += 1
            res = max(res,r - l)
            l = r
        return res
```
