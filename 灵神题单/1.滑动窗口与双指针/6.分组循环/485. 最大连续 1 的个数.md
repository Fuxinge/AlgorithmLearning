```python
class Solution:
    def findMaxConsecutiveOnes(self, nums: List[int]) -> int:
        res = cnt = 0
        n = len(nums)

        for c in nums:
            if c:
                cnt += 1
                res = max(res,cnt)
            else :
                cnt = 0

        return res
```
