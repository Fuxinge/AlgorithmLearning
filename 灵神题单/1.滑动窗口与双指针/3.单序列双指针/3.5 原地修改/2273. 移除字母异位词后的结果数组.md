```python
class Solution:
    def removeAnagrams(self, words: List[str]) -> List[str]:
        n = len(words)
        idx = 1
        for s,t in pairwise(words):
            if Counter(s) != Counter(t):
                words[idx] = t
                idx += 1
        del words[idx :]
        return words

```
