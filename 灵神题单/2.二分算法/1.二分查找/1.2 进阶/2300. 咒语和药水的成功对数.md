```python
class Solution:
    def successfulPairs(self, spells: List[int], potions: List[int], success: int) -> List[int]:
        n,m = len(spells),len(potions)
        potions.sort()

        res = [0]  * n
        for i in range(n):
            if success % spells[i] != 0:
                spells[i] = success // spells[i] + 1
            else :
                spells[i] = success // spells[i]
        for i,c in enumerate(spells):
            res[i] = m - bisect_left(potions,spells[i])
        return res
```
