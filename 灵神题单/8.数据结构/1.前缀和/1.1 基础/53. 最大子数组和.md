求前缀和，之后是买卖股票的最佳时机

```python
class Solution:
    def maxSubArray(self, nums: List[int]) -> int:
        s = list(accumulate(nums,initial = 0))
        mi = inf
        res = -inf
        for c in s:
            res = max(res,c - mi)
            mi = min(mi,c)

        return res
```

还可以用dp做
