```python
class Solution:
    def maximumTripletValue(self, nums: List[int]) -> int:
        res = 0
        prema = 0
        diff = 0
        for k,c in enumerate(nums):
            res = max(res,c * diff)
            diff = max(diff,prema - c)
            prema = max(prema,c)

        return res

```
