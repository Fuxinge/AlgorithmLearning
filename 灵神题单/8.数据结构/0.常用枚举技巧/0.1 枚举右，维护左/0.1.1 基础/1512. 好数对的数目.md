```python
class Solution:
    def numIdenticalPairs(self, nums: List[int]) -> int:
        res = 0
        n = len(nums)
        mp = defaultdict(int)
        for i,c in enumerate(nums):
            if c in mp:
                res += mp[c]
            mp[c] += 1

        return res
```
