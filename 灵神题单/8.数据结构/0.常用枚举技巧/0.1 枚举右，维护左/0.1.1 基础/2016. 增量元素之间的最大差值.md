```python
class Solution:
    def maximumDifference(self, nums: List[int]) -> int:
        n = len(nums)
        res = -1
        cur = inf
        for c in nums:
            cur = min(cur,c)
            if c > cur :
                res = max(res,c - cur)
        return res
```
