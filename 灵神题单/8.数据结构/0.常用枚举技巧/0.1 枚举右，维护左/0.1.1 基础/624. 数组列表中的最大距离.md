```python
class Solution:
    def maxDistance(self, nums: List[List[int]]) -> int:
        mn = inf
        mx = -inf
        res = 0
        for c in nums:
            res = max(res,mx - c[0],c[-1] - mn)
            mn = min(mn,c[0])
            mx = max(mx,c[-1])

        return res
```
