```python
class Solution:
    def minimumCardPickup(self, cards: List[int]) -> int:
        res = inf
        mp = defaultdict(int)
        for i,c in enumerate(cards):
            if c in mp:
                res = min(res,i - mp[c] + 1)
            mp[c] = i

        return res if res != inf else -1
```
