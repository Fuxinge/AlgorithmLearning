```python
class Solution:
    def findMaxK(self, nums: List[int]) -> int:
        res = -1
        mp = defaultdict(int)
        for i,c in enumerate(nums):
            if -c in mp:
                res = max(res,abs(c))
            mp[c] = 1

        return res
```
