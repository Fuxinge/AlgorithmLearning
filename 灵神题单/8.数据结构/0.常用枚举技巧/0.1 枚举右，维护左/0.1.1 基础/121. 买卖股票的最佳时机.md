```python
class Solution:
    def maxProfit(self, nums: List[int]) -> int:
        n = len(nums)
        res = 0
        cur = inf
        for c in nums:
            cur = min(cur,c)
            res = max(res,c - cur)
        return res
```
