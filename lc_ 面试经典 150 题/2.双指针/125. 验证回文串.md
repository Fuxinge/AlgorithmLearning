```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        l = []

        for c in s.lower():
            if c in ascii_lowercase or c.isdigit():
                l.append(c)

        return l == l[::-1]
```
