```python
class Solution:
    def canJump(self, nums: List[int]) -> bool:
        res = 0
        n = len(nums)
        for i in range(n):
            if i <= res:
                res = max(res,i + nums[i])
        return res >= n - 1
```
