```python
class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        n = len(s)
        cnt = 0
        while n-1 >= 0 and s[n-1] == ' ':
            n -= 1
        while n-1 >= 0 and s[n-1] != ' ':
            n -= 1
            cnt += 1
        return cnt
```
