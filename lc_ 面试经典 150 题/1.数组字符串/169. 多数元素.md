```python
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        dummy = 0
        cnt = 0
        for c in nums:
            if cnt == 0:
                cnt = 1
                dummy = c
            elif dummy == c:
                cnt += 1
            else :
                cnt -= 1

        return dummy
```
