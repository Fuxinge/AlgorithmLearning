```python
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        k = 0
        n = len(nums)
        i = 0
        while i < n:
            while i + 1 < n and nums[i] == nums[i + 1]:
                i += 1
            nums[k] = nums[i]
            i += 1
            k += 1
        return k
```
